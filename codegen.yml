overwrite: true

schema: "https://api-sa-east-1.hygraph.com/v2/clk011n8m0qlj01uh4a4ngtem/master"

documents: "./src/graphql/**/*.graphql"

generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
    config:
      withHooks: true

hooks:
  afterAllFileWrite:
    - eslint --fix
